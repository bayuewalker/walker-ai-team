"""Phase 10.2 — Execution validation package.

Modules:
    fill_tracker     — Real fill tracking with slippage and latency.
    reconciliation   — Order ↔ fill reconciliation and ghost-position guard.
    simulator        — PAPER_LIVE_SIM mode using live orderbook data.
"""
